import 'package:flutter/material.dart';

class TwoFacesScreen extends StatelessWidget {
  const TwoFacesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('two Faces'),
      ),
    );
  }
}
